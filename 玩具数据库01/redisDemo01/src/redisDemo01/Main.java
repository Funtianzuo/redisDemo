package redisDemo01;

import java.util.*;

public class Main {

	public static void main(String[] args) {
		// Scanner in=new Scanner(System.in);
		// redis r0=new redis();
		// r0.read();
		// while(!r0.command(in.nextLine()).equals("end"));
		socket aSocket = new socket();
		aSocket.run();
	}
}
